import urllib.request, ssl, json; ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE; req = urllib.request.Request('https://vd.mackolik.com/livedata?date=27/08/2026', headers={'User-Agent': 'Mozilla/5.0'}); data = json.loads(urllib.request.urlopen(req, context=ctx).read().decode('utf-8')); print('Events count:', len(data.get('e', []))); print('First event:', data.get('e', [])[0])
